defmodule Floofcatcher.Ctftime.Api do
  def get_event(id) do
    :not_implemented
  end

  def get_team(id) do
    # TODO: We can't use the API endpoint since it doesn't return
    # the description. :(
    :not_implemented
  end
end
